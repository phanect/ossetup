language: bash
sudo: required
services:
  - docker

before_install:
  - docker build --tag=phanect/ossetup .
  - docker run phanect/ossetup /bin/bash

script:
  - curl https://raw.githubusercontent.com/phanect/ossetup/master/setup.sh | bash
